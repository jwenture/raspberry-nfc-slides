sudo apt update
sudo apt upgrade
sudo apt install swig liblgpio-dev


python3 -m venv venvnfcslides
source venvnfcslides/bin/activate
pip install adafruit-circuitpython-debug-i2c
pip install adafruit-circuitpython-pn532

___
Error:
ValueError: No Hardware I2C on (scl,sda)=(3, 2)
Valid I2C ports: ((1, 3, 2), (0, 1, 0)).
Make sure I2C is enabled.



sudo raspi-config

## We are no longer using I2C so ignore this
#Then navigate through the menus:
#Select 3 Interface Options (or "Interfacing Options" on older versions).
#Select I5 I2C.
#Choose Yes when asked "Would you like the ARM I2C interface to be enabled?"
#Choose Yes if it asks about automatically loading the kernel module.
#Select Finish, and when prompted, choose Yes to reboot your Pi. 

sudo raspi-config

Use SPI instead
Select 3 Interface Options (or "Interfacing Options").

Select I4 SPI.

Choose Yes when asked "Would you like the SPI interface to be enabled?".

Choose Yes if prompted to load the kernel module.

Select Finish, then reboot your Pi when prompted .


__________________
ISSUES, CANNOT DETECT NFC

sudo apt install libnfc-bin

$ nfc-scan-device -v
nfc-scan-device uses libnfc 1.8.0
No NFC device found.


sudo nano /etc/nfc/libnfc.conf
device.name = "_PN532_SPI"
device.connstring = "pn532_spi:/dev/spidev0.0:500000"


(venvnfcslides):~/raspberry-nfc-slides/nfc-test $ nfc-scan-device
nfc-scan-device uses libnfc 1.8.0
1 NFC device(s) found:
error   libnfc.driver.pn532_spi Unable to wait for SPI data. (RX)
pn53x_check_communication: Timeout
error   libnfc.driver.pn532_spi Unable to wait for SPI data. (RX)
nfc_open failed for pn532_spi:/dev/spidev0.0:500000


THERE IS A VERY GOOD CHANCE THIS IS DUE TO THE WIRES NOT CONNECTED WELL

LOOK UP SEL0 and SEL1 switch. It is a super small switch on the board. LOOK FOR IT

____

 nfc-scan-device
nfc-scan-device uses libnfc 1.8.0
1 NFC device(s) found:
- _PN532_SPI:
    pn532_spi:/dev/spidev0.0:100000